language: python
python:
  - "3.6"

services:
  - docker

before_script: 
- pip install docker-compose
- chmod +x Test.sh

jobs:
  include:
    - stage: build
      name: "Build"
      script: 
      - docker build -t flask-api:latest .
    - stage: test
      script: 
      - docker run -i -p 5000:5000 flask-api

